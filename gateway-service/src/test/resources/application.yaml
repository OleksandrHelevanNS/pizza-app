spring:
  application:
    name: gateway-service
  cloud:
    gateway:
      server:
        webflux:
          discovery:
            locator:
              lower-case-service-id: true
              enabled: true
server:
  port: 0
token:
  secret: this-is-a-very-secure-secret-key-for-jwt-token-validation-in-gateway-service
