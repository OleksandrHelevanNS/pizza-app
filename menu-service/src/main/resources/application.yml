server:
  port: ${SERVER_PORT:8081}

spring:
  application:
    name: menu-service

  cassandra:
    keyspace-name: menu
    contact-points: cassandra:9042
    local-datacenter: datacenter1
    schema-action: create_if_not_exists
    request:
      timeout: 10s
    connection:
      connect-timeout: 10s
      init-query-timeout: 10s

logging:
  level:
    root: INFO
    com.ing.data.cassandra.jdbc: INFO